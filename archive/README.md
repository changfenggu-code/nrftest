# nrftest 归档

此目录保存被新方案取代的完整项目状态，不是活动实现。

## Connectivity/Blatann 基线

`connectivity-blatann-2026-09-06/` 保存切换到 BTP-first 方案之前的全部内容，包括：

- 原计划；
- Python Host 实现和测试；
- GATT Profile；
- Nordic Connectivity firmware；
- `pc-ble-driver-py` wheel 和 Blatann 环境配置；
- Pixi、Just、pytest 配置；
- 当时存在的本地缓存和辅助资产。

该归档的历史 Windows/COM11 验证只适用于旧方案，不能作为当前 BTP 主线的通过证据。

当前活动计划位于 `../docs/PLAN.md`。
